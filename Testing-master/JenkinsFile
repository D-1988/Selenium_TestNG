pipeline {
    agent {
        node {
            label 'master'
              }
           }

          options {
                timestamps()
          }

          stages {
            stage("Checkout , Test & Publish"){
                checkout scm
                script {
                    bat(/mvn clean test/)
                }
                step ([$class: 'Publisher' , reportFileNamePattern:'**/testng-results.xml'])
            }
          }
}